package tcp_uno.game;

/**
 * <code>CardColor</code> is an enumeration that stores all the possible values
 * for <code>Card</code> colors
 */
public enum CardColor {
    RED,
    GREEN,
    BLUE,
    YELLOW,
    BLACK
}
